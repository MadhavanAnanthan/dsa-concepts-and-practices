# DSA Pattern Revision Table

Use this as a quick revision index. The **Pattern** is the parent category, and the **Sub-pattern** is the more specific technique to recall before solving the problem again.

| Pattern | Sub-pattern | Unique problem statement | Revision hint |
|---|---|---|---|
| **Hashing** | **1.** Complement lookup<br><br>**2.** Set intersection<br><br>**3.** Seen-before set<br><br>**4.** Last-seen index map | **1. Pair Sum / Two Sum** — Find two values or indices whose sum equals a target.<br><br>**2. Intersection of Two Arrays** — Return the unique values present in both arrays.<br><br>**3. 217. Contains Duplicate** — Determine whether any value occurs more than once.<br><br>**4. 219. Contains Duplicate II** — Find equal values whose indices differ by at most `k`. | **1.** Compute `target - current`; use a `HashSet` for values or a `HashMap<value, index>` for indices.<br><br>**2.** Put one array in a set, scan the other, and collect matches in a result set.<br><br>**3.** Only presence matters: if `set.add(value)` fails, a duplicate exists.<br><br>**4.** Store the latest index; check `currentIndex - previousIndex <= k`, then update it. |
| **Two Pointers** | **1.** Opposite ends on sorted input<br><br>**2.** Opposite-end swapping<br><br>**3.** Backward in-place merge<br><br>**4.** Opposite ends by absolute value<br><br>**5.** Same-direction subsequence scan<br><br>**6.** Same-direction slow/fast read-write compaction<br><br>**7.** Opposite ends with filtering | **1. Two Sum II — Input Array Is Sorted** — Find the 1-based indices of two numbers whose sum equals the target.<br><br>**2. Reverse Array / String** — Reverse the elements in place.<br><br>**3. Merge Sorted Array** — Merge `nums2` into the extra space in sorted `nums1`.<br><br>**4. 977. Squares of a Sorted Array** — Return the squares in nondecreasing order.<br><br>**5. 392. Is Subsequence** — Check whether all characters of `s` occur in order within `t`.<br><br>**6a. Remove Duplicates from Sorted Array** — Remove duplicates in place and return the number of unique elements.<br><br>**6b. Move Zeroes** — Move all zeroes to the end while preserving the order of non-zero values.<br><br>**7. Valid Palindrome** — Ignore non-alphanumeric characters and case, then test whether a string is a palindrome. | **1.** Sum both ends; move `right` for a smaller sum and `left` for a larger sum.<br><br>**2.** Swap `left` and `right`, then move both inward until they meet.<br><br>**3.** Compare the largest remaining values and fill from the final index backward.<br><br>**4.** The largest absolute value is at an end; write the larger square from right to left.<br><br>**5.** One pointer waits for the next character of `s`; the other always advances through `t`.<br><br>**6a.** The first value is already unique: `slow = 0`, `fast = 1`; advance `slow` when a new value is found.<br><br>**6b.** Every value must be read: `slow = 0`, `fast = 0`; write or swap each non-zero at the next valid position.<br><br>**7.** Skip invalid characters at both ends, compare lowercase characters, and move inward. |
| **Traversal** | **1.** Running maximum<br><br>**2.** Adjacent comparison<br><br>**3.** Track top two distinct values<br><br>**4.** Running streak count | **1. Find Maximum in Array** — Find the largest array element.<br><br>**2. Check if Array Is Sorted** — Determine whether an array is in nondecreasing order.<br><br>**3. Second Largest Element** — Find the second-largest distinct array value.<br><br>**4. Maximum Consecutive Ones** — Find the longest contiguous run of `1`s. | **1.** Initialize from the first element and update the maximum during one scan; do not sort.<br><br>**2.** Compare each element with its predecessor; one inversion means the array is unsorted.<br><br>**3.** Maintain `max` and `secondMax`; when a new maximum arrives, demote the old maximum.<br><br>**4.** Increment the current streak for `1`, reset it for `0`, and preserve the best streak. |
| Greedy | Running minimum | **Best Time to Buy and Sell Stock** — Maximize profit using one buy followed by one sell. | Track the lowest earlier price and maximize `currentPrice - minPrice`. |
| **Binary Search** | **1.** Exact match<br><br>**2.** Lower-bound style<br><br>**3.** Boundary search | **1. Normal Binary Search** — Find a target's index in a sorted array.<br><br>**2. Search Insert Position** — Return the target index or its sorted insertion position.<br><br>**3. First and Last Position** — Find the first and last occurrences of a target in a sorted array. | **1.** Compare at `mid`; discard half the search space and return immediately when found.<br><br>**2.** Run binary search; if absent, `start` is the insertion index after the loop.<br><br>**3.** On a match, save `mid`; continue left for the first boundary and right for the last. |
| Frequency Counting | Fixed character range | **Valid Anagram** — Determine whether two strings contain identical character counts. | Increment for the first string and decrement for the second; all counts must finish at zero. |
| Bit Manipulation | XOR cancellation | **Single Number** — Find the one value that appears once when every other value appears twice. | XOR every value: equal pairs cancel because `x ^ x = 0`. |
| Math / Bit Manipulation | Expected range difference / XOR | **Missing Number** — Find the missing value from the range `0..n`. | Subtract actual values from the expected sum, or XOR `0..n` with all array values. |
| String Traversal | Horizontal or vertical prefix scan | **Longest Common Prefix** — Find the longest starting substring shared by all strings. | Either shrink a prefix string by string, or compare the same character index across every string. |
| Prefix Sum | Total sum plus running left sum | **724. Find Pivot Index** — Find an index whose left sum equals its right sum. | Compute `rightSum = totalSum - leftSum - current`; update `leftSum` only after checking. |
| Sliding Window | Fixed-size window | **643. Maximum Average Subarray I** — Find the maximum average among contiguous subarrays of length `k`. | Build the first sum, then subtract the outgoing value and add the incoming value; divide once at the end. |
| Stack | Matching pairs / nested structure | **20. Valid Parentheses** — Verify that brackets are correctly matched and nested. | Push openings; for each closing bracket, require and pop its matching top; finish with an empty stack. |
